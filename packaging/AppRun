#!/bin/sh
# Entry point inside the AppImage. rsync itself is deliberately not bundled:
# it is a system tool and the host's copy is the one the user expects.
HERE="$(dirname "$(readlink -f "$0")")"
exec "${HERE}/usr/bin/rsync-ui" "$@"
